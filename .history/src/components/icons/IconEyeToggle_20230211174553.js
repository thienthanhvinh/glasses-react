import React from 'react';

const IconEyeToggle = ({open = false, onClick = () => {}}) => {
    if(open) 
    return (
        <span onClick={on}>
            
        </span>
    );
};

export default IconEyeToggle;